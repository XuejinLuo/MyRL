defaults:
  - config
  - _self_
q_selection:
  checkpoint: ${paths.root}/critic/checkpoints/epoch_0010.pth
  output: ${paths.root}/q_selection_clipping_ablation
  ablation: true
  split: diagnostic
  candidates: [1, 8]  # Used only with ablation=false (historical comparison).
  sampler: cps
  seed_start: 4000
  episodes: 100
